# Archived Code

`SoftwarePWM/` contains the previous MCP23017/native-pin software PWM implementation.
It is preserved here for reference and possible extraction into a separate repo, but
it is no longer in PlatformIO's active `lib/` build path and is not used by the
receiver motor-control firmware.
